<div class="session-section">
  <h1>Reset Password</h1>
  <%= form_tag(password_resets_path) do %>
    <%= text_field_tag :email, params[:email], required: true, placeholder: 'Email'%><p>
    <%= submit_tag 'Sign in' %>
  <% end %>
</div>