<div id="pip-form">
  <%= form_for(@pip) do |f| %>
    <% if @pip.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@pip.errors.count, "error") %> prohibited this pip from being saved:</h2>
        <ul>
        <% @pip.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    <div class="field">
      <%= f.text_area :body, placeholder: "whats pippin?", id: "body-input-field" %> <br>
    </div>
    <div class="actions">
      <%= f.submit "Pip", id: 'pip-submit-button' %>
    </div>
  <% end %>
<div>